#!/usr/bin/env python3
"""This module provides a function to add two floating-point numbers.
It takes two float arguments and returns their sum as a float."""


def add(a: float, b: float) -> float:
    """Returns float: The sum of the two input numbers."""
    return a + b
