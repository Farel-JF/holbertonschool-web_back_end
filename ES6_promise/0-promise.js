export default function getResponseFromAPI() {
  // The function returns a new Promise object
  return new Promise((resolve) => {
    // The resolve function is called immediately, fulfilling the promise
    resolve();
  });
}
